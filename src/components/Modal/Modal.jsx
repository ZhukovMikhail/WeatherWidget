import { BackDrop } from './Modal.styled';
import PropTypes from 'prop-types';

export const Modal = () => {
  return (
    <>
      <BackDrop></BackDrop>
    </>
  );
};

// MyForm.propTypes = {
//   id: PropTypes.string,
// };
